import { PageLoader } from "@/components/feedback/page-loader";

export default function Loading() {
  return (
    <PageLoader
      title="Loading SynapseOS"
      description="Initializing the cinematic control layer, syncing route state, and bringing your next workspace online."
    />
  );
}
