import { PageLoader } from "@/components/feedback/page-loader";

export default function DashboardLoading() {
  return (
    <PageLoader
      title="Loading dashboard"
      description="Hydrating secure role-aware modules, chart surfaces, and operational summaries for this workspace."
    />
  );
}
